services:
  - type: web
    name: pattygpt
    env: python
    buildCommand: ""
    startCommand: python app.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # We'll add this manually in Render dashboard
